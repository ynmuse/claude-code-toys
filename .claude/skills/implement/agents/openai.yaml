interface:
  display_name: "Implement"
  short_description: "Implement with per-outcome reconciliation and one review pass"
  default_prompt: "Use $implement to implement this spec folder one outcome at a time, reload current repository evidence and reconcile observed behavior with the product contract and active unfinished tasks after each outcome, treat superseded tasks only as implicated history, then complete verification, review the whole diff once and triage what it returns, and give a runnable handoff when available."
